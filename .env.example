VITE_ADMIN_PIN=your-4-digit-admin-pin-here

# Create a local `.env` file with the real secret and ensure it's ignored by git.
# Example `.env` (DO NOT COMMIT):
# VITE_ADMIN_PIN=5394

# For Vite, environment variables that should be exposed to client code must be prefixed with VITE_.